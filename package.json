{
  "name": "where-they-began-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static one-page subscribe funnel for Where They Began.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css dist/",
    "preview": "python3 -m http.server 4173 --directory dist",
    "serve": "python3 -m http.server 4173"
  }
}
